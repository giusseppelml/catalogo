<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="owl.app.catalogo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AdministradorActivity" />
        <activity android:name=".activities.VerProductosActivity" />
        <activity android:name=".activities.RegistrarseActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.VentasEspecificasActivity" />
        <activity android:name=".activities.PerfilActivity" />
        <activity android:name=".activities.CarritoActivity"></activity>
    </application>

</manifest>